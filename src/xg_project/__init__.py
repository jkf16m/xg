"""xg — a TUI for deterministic agents.

The package is small on purpose. ``graph`` says what nodes exist and who may
move between them; ``session`` holds where one conversation is; ``app`` draws it.
"""

__version__ = "0.1.0"
